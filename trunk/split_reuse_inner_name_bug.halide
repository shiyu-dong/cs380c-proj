#include "Halide.h"
#define RESULT_TYPE int
using namespace Halide;

int main(int argc, char **argv) {
    Var x;//, xo;

    Func f(x);

    f(x) = 1;
    //f.root().split(x, xo, x, 2);
    f.realize(30);

    printf("Success!\n");
    return 0;
}
